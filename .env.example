# PrivateBrain — demonstration environment.
#
# The prototype runs entirely client-side against seeded synthetic data.
# No environment variables are required. This file exists so evaluators can see
# that no secrets are used anywhere in the demo.
#
# Optional (unused by default):
# VITE_API_BASE=http://localhost:8000
