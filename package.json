{
  "name": "privatebrain",
  "private": true,
  "version": "1.0.0",
  "description": "PrivateBrain — the private intelligence layer for the AI-native enterprise (ideathon prototype)",
  "scripts": {
    "install:all": "cd frontend && npm install",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "typecheck": "cd frontend && tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  }
}
